5个谓词get post delete put patch

get 语意偏向于查询
post 偏向于创建
put 的话是全量更新，把一整个资源对象的字段全传过来才行

现在有的接口是
/ping
/v1/token
/v1/user

关于ping，用来做心跳包，不用带token了（上次说错了，ping和token没关系），他只是用来测试用户是否断链，断了就显示服务器重连中，如果3-4次ping失败了，
就把他踢出去了（让他可以选择返回主菜单）
ping我们3秒一次就可以，频率太高会对服务器负载太大，（他第一次ping失败了，就可以提高ping的频率，变成1秒，3-4次ping
失败了，就让用户选择返回主页面或者再次尝试重连）
ping还有一个作用，就是你可以根据ping这个接口你发送请求的时间节点减收到response的时间节点，来计算延迟，给用户显示，
你的逻辑也可以是，比如延迟超过800ms就算是断链

token接口就get和post
post接口他是用来创建一个token也就是登陆
然后你把token存在本地，每次用户打开你的这个客户端（可执行文件），包括重开的第一时间check本地的token是否失效
检查的接口是token.get，返回成功的话，就不用成功登陆。
刚才说到重连，用户有时候会把进程删掉，重新打开客户端，这个时候godot肯定会默认打开主场景，所以你要在本地储存一个
用户的状态信息，回溯到正在进行的对局，当然是否在进行对局和具体的对局信息的接口后面我会提供，现在还没写




